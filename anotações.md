**mapState** -> mapear o state
**acessar o estado** -> *this.$store.state.funcao*


```javascript
store({
    state: {
        //dados da aplicação
    },
    mutations: {
        //alterar esses dados (state
    },
    getter: {
        //funções ou metodos que serão chamados em qualquer componente
    },
})
```